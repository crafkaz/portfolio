export interface ThemeConfig {
  bgColor: string;
  cardBg: string;
  subtleBg: string;
  tagBg: string;
  borderColor: string;
  textColor: string;
  mutedColor: string;
  accentColor: string;
  navBg: string;
  gridDot: string;
  glow: string;
}
